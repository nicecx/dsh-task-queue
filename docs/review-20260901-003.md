# 20260901-003 修订重提：复用评估强制关卡 v3（响应 002 三意见）

- requestId: 20260901-003（修订 20260901-002 changes-requested 后重提）
- 类型: design（复用评估强制关卡 v3）

## 002 意见逐条落实

### 1. [阻塞] reuseCheck 数组填充来源未定义 → checkReuseSection 返回引用分类
**修正**：
```js
// core.js
export function checkReuseSection(content) {
  // 返回 { ok, reason, references: { index: [], awesome: [], github: [] } }
  // 抽取规则：节内命中 "CAPABILITY-INDEX" → index；"awesome"（大小写不敏感）→ awesome；
  // "github.com" → github；每个数组存命中行原文（截断 80 字符）
}
```
- submit() 将 `references` 传入 buildRequest 的 `reuseCheck`（不再恒空，Hermes 可核对一致性）
- 逃逸口通过时 references 可空数组（audit 记 reuse-escape）

### 2. [阻塞] auto-detect 路径静默拒绝 → 拒绝时 deliver 原因 + audit reuse-reject
**修正**（index.js write/edit 自动识别分支）：
```js
const r = checkReuseSection(content)
if (!r.ok) {
  audit({ action: 'reuse-reject', filePath, reason: r.reason })
  deliver(sessionId, `⚠️ 设计文档未通过复用评估关卡：${r.reason}\n请补充「## 复用评估」节（三查：CAPABILITY-INDEX / awesome / GitHub；全新功能可声明「无复用」）。`)
  return next()  // 仍放行写入，但提审被拦——作者明确知道原因
}
```
- design_review_submit 工具路径：返回 ⚠️ r.note 文案（已有模式）
- 两条路径都让作者收到"为何被拒 + 三查提示 + 逃逸声明写法"

### 3. [小] 大小写不敏感测试用例笔误 → 落在引用/关键词 + 英文节名变体
**修正**（test/index.test.js 用例清单）：
- 节名英文变体：`## Reuse Evaluation`（大小写不敏感）→ ok
- 引用大小写：`capability-index` / `GitHub.com` / `awesome` 任意大小写 → ok
- 中文节名 `## 复用评估` 本身无大小写概念，仅测存在性

### 4. [建议·非阻塞] 逃逸口假阳性 → audit reuse-escape 复核
- 保留 substring 判定（简单），但每个逃逸通过都 audit 记 `reuse-escape`（含 docPath/reason），供人工抽查；Hermes 评审核对 reuseCheck 与文档一致性时自然复核

## 实施清单（approved 后）

1. core.js：`checkReuseSection(content)` 返回 { ok, reason, references } + buildRequest 接 reuseCheck
2. index.js：submit() 关卡调用（工具路径返回 ⚠️ note；auto-detect 路径拒绝时 deliver 原因 + audit reuse-reject）
3. test/index.test.js：用例更新（ok/缺节/大小写引用/英文节名/逃逸口/空节 + references 分类断言）
4. CAPABILITY-INDEX.md 落盘 ~/.dsh/
5. 契约"开发前复用检索"条款（节名统一"复用评估"）

## 变更文件

- dsh-design-review/src/core.js / src/index.js / test/index.test.js
- ~/.dsh/CAPABILITY-INDEX.md（新建）
- ~/.dsh/DSH-HERMES-CONTRACT.md（条款）

## 测试

- node --test test/index.test.js（10 旧 + 新用例全绿）
