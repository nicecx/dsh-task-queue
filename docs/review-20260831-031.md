# 031 修订重提：swap 阈值 80→95（对齐 resource_gate 先例，彻底消除误报）

- requestId: 20260831-031（修订 20260831-030 approved 的 80→90，用户明确要求"非问题不报"）
- 类型: design（监控配置调整修订）

## 030 结论回顾

030 approved（80→90），审核实测确认：H2=86% 时 H7 换页速率 0.0 页/秒（零颠簸）→ **swap 高位 + 零换页 = 正常交换，属误报**；resource_gate 先例 DEFAULT_SWAP_USED_PCT=95.0 属实。

## 031 修订：80 → 95（一次到位）

用户明确要求："如果不是什么问题，类似这样的问题就不要报了。"

| 项 | 030（已批） | 031（修订） |
|---|---|---|
| swap_used_percent | 80 → 90 | **80 → 95** |
| 依据 | 保留 5pt 灵敏度 | **对齐 resource_gate 95.0 先例**（8GB 机型 swap 常态 70-86%，90 仍留误报空间） |
| 其他阈值 | 不动 | 不动（disk 85 / memory 20 / load 12） |
| 真危机判定 | H7 换页速率兜底 | H7 换页速率兜底（swap_page_rate 50，独立不受影响） |

实测数据支撑：09-01 00:00=83%、03:00=73%、06:00=84%、06:16 审核实测=86%——全部 <95，**95 阈值下该机型常态永不触发**；真危机（换页激增）仍由 H7 告警。

## 附加建议（非阻塞）：配置一致性维护

resource_gate.py 注释声称"与 inspection.json 对齐"，但两文件此前实际漂移（80 vs 95）——正是"调了几次一直有"的根因。建议：
- 两文件阈值同源维护：改一边必须同步另一边（本次 031 即同步为 95）
- inspection.json 纳入版本管理（当前无 git，改错无法回滚）——列为后续 TODO 另行处理

## 变更文件

- ~/Documents/运维快照/harness/inspection.json：`thresholds.swap_used_percent: 80 → 95`

## 验证

改后手动跑 `env -u PYTHONPATH python3 ~/.hermes/scripts/l0_sentinel.py` → 期望 rc=0 静默（当前 swap ≈77-86% < 95）；观察下一轮 3h 哨兵无告警。
