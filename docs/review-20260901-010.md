# 20260901-010 lesson 主动触发制度（发现即自动提审，不再依赖自觉）

- requestId: 20260901-010
- 类型: design（用户询问"主动触发 lesson learnt 是否制度化"——机制已建、触发未制度化，本次固化）

## 复用评估

- 本地 CAPABILITY-INDEX：本方案为 **dsh-design-review（lesson 机制）既有能力的制度扩展**（lesson_review_submit/审核/传播已存在，本次固化触发条款），非新造插件
- 外部收录（awesome）：无对应条目
- GitHub 借鉴：无（制度条款类变更，无外部方案可借鉴）
- 无复用：全新制度条款（触发场景清单 + 自动提审流程），无既有资产可复用

## 背景

lesson 机制（lesson_review_submit → Hermes 审核 → 守则追加 + 传播扫描）已实战闭环（009），但**触发依赖 agent 自觉**——用户多次指出"发现问题后应该自动"（教训：口头承诺不可靠，须条款化）。

## 制度条款（写入契约「四·十一 lesson 主动触发」）

1. **触发场景清单**（满足任一 → 必须 lesson）：
   - 关卡/机制被绕过（如 009：异常放行、导入缺失）
   - 审批链异常（裁决失效/静默放行/fail-closed 误判）
   - 队列/消费端卡死或误判（如 fire 锁陈旧、单槽污染）
   - 部署事故（sed 静默失败类、重启失败、健康检查异常）
   - 数据/安全事件（权限绕过、凭据风险、不可逆操作）
   - 告警风暴或反复误报（如 swap 阈值误报链条）
   - 任何 S1/S2 级缺陷修复完成后

2. **自动提审流程**（无需用户确认）：
   - 事件确认后 **24h 内** lesson_review_submit 提审（title/incident/rootCause/lesson/defectPattern 必填）
   - defectPattern 必填（传播扫描依赖；可后续优化提取质量）
   - changeFiles 填实际修复源码路径（009 审核非阻塞观察）

3. **闭环衔接**：
   - approved → 守则追加（OPS-GUARDRAILS）+ 传播扫描（已有）
   - changes-requested/rejected → 修订重提（同互审流程）
   - 修复验证后更新 KNOWN-ISSUES（open→fixed→verified）

4. **遗漏问责**：每日收盘核对（004 已有 schedule 纪律）顺带检查"当日 S1/S2 事件是否已 lesson"；遗漏 → 补提 + audit 记录

## 方案

契约新增「四·十一 lesson 主动触发」条款（上述制度条款 1-4 原文写入），版本 +0.1（1.2 → 1.3）；本方案无代码改动（纯契约条款）。

## 接口与兼容性

- 无协议/接口变更：lesson_review_submit 工具与审核流程不变，仅新增"何时必须调用"的制度约束
- 向后兼容：现有 lesson 流程不受影响；条款只加不删

## 安全

- 权限边界：无新权限（契约文档变更）
- 敏感数据：无（条款文本不含密钥/凭据）
- fail-closed：不适用（非裁决路径）；遗漏问责机制兜底"未提审"风险

## 测试方案

- T1：未来任一 S1/S2 事件发生后，24h 内自动出现 lesson 提审（观察）
- T2：回填验证——031 swap 误报链（已修复未 lesson）作为制度生效后首条回填
- T3：遗漏检查——每日收盘核对发现未提审 → 补提 + audit 记录

## 变更文件

- ~/.dsh/DSH-HERMES-CONTRACT.md（新增四·十一 + 版本 +0.1）

## 风险与回滚

- 风险：条款不生效（agent 仍遗漏）→ T3 遗漏核对兜底 + 抽查
- 回滚：删除四·十一 条款 + 版本回退（文档级，无代码回滚）

## 验证

- 未来 S1/S2 事件发生后自动出现 lesson 提审（无需提醒）
- 本制度生效前的遗留事件（如 031 swap 误报链）可作首条回填验证
