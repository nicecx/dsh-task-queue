# 028 修订重提：退役章节更正（027 阻塞：退役目标 = DSH 直写路径，非审核 cron）

- requestId: 20260831-028（修订 20260831-027 changes-requested 后重提）
- 类型: design（阶段 3 收尾文档更正）
- 代码状态: 阶段 3 全部代码已 approved 并部署（消费端 14/14、design-review 10/10、auto-approver 27/27、reset-handoff 13/13）；本请求同样走队列全链路实证

## 027 阻塞项更正

### 退役目标 = DSH 直写 request.json 的旧投递路径（已退役，实质完成）
- 代码核对（027 审核确认）：dsh-design-review/src/index.js 的 submit 只写 queue.json，全文件无任何直写 request.json 语句；
  review_submit.py 同样只入队。**DSH 侧直写投递路径已在阶段 3 全部移除** → 退役实质已完成，无需任何额外动作。

### 保留目标 = Hermes 审核 cron 09a5c5dd5e05（dsh-review-inbox，必须保持 enabled）
- 该 cron 是 Hermes 评审入口（monitor=dsh_review_inbox_monitor.py，schedule */2），负责读 request.json 审核并写 result.json。
- **不执行任何 disabled/暂停/删除动作**；027 文档第 3 节"暂停（enabled=false）"为错误表述，予以更正。

## 第 3 节更正后（退役确认，已完成）

| 对象 | 动作 | 状态 |
|---|---|---|
| DSH 直写 request.json 投递路径（design_review_submit 旧版 / review_submit.py 旧版） | 已由阶段 3 入队改造替代 | ✅ 已退役（代码核实无直写残留） |
| Hermes 审核 cron 09a5c5dd5e05（dsh-review-inbox，评审入口） | 保持 enabled，不动作 | ✅ 保留（必须） |
| task-queue 消费端 cron 4082e601f231 | 运行中（*/1 + monitor 门控 + 钉 flash） | ✅ 保留（投递新入口） |

## 全链路闭环（本请求实证）

本请求 028 经：新版 design_review_submit 入队 → 消费端出队 → 写 request.json + docs 落盘 → Hermes 审核 cron 评审 → result 注入。同 027 已验证的前半段 + 本请求的评审闭环。

## 遗留 TODO（非本轮，同 027）

- reset_agent.py read_request 补 id/reason 校验（另行提审）
- approve 快道真实裁决 E2E 观察
- reset tier E2E（低风险时机）
