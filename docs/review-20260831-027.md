# 027 阶段 3 完成报告与退役确认（026 approved 方案收尾实证）

- requestId: 20260831-027
- 类型: design（026 approved 阶段 3 的收尾实证 + review-inbox 退役报备）
- 前置: DSH 已重启加载插件新版本（reset 40b541db，健康全过）；本提审本身即走队列全链路实证

## 1. 本提审 = 全链路实证

本请求由**新版 design_review_submit**（入队模式）提交：
- 入队 queue.json（tier=review）→ 消费端 cron 出队 → 写 request.json + docs 落盘 → Hermes 审核 → result
- 若 Hermes 看到本 requestId 正常出 result，即证明阶段 3 全链路闭环（026 退役条件：一轮真实提审全链路实证通过）

## 2. 阶段 3 实施汇总（026 approved 全部落地）

| 步 | 机制 | 改动 | 验证 |
|---|---|---|---|
| 1 | review_submit.py | 提审入队 tier=review（保留 docs/ 取号 + 冲突断言 + 快照） | 离线全链路 E2E ✅（入队→出队→request.json+docs 落盘） |
| 2 | dsh-design-review | submit 入队 + 取号扫 docs/（007 单一源）+ 快照幂等 + drainQueue 过渡转投 | 10/10 单测 ✅；本提审即实证 |
| 3 | dsh-auto-approver | runApproveTask：入队 approve + busy 锁（共用 .hermes-busy）+ 快道认领 + results/ 落盘 | 27/27 单测 ✅（部署后 E2E） |
| 4 | dsh-reset-handoff | reset_handoff 入队 reset（payload id/reason/scope）；入队失败 fallback 直写 | 13/13 单测 ✅ |
| 5 | 消费端 | pickNext 过滤 approve + 单槽（026 修正版）+ doc 落盘 + reset 调权威 agent（timeout=600）+ 异常兜底 | 14/14 单测 ✅ + 部署回归 ✅ |

消费端 cron 已运行（*/1 + monitor 门控 + 钉 deepseek-v4-flash，健康基线 e3b0c442）。

## 3. 退役申请：review-inbox cron（09a5c5dd5e05）

- 条件满足（026 第 4 节）：步骤 1-2 完成 + 本提审全链路实证通过后
- 动作：暂停（enabled=false，保留配置可回滚）→ 观察一轮（本请求审核闭环无回归）→ 删除或长期暂停
- 替代：消费端出队后写 request.json，Hermes review-inbox 依旧能审（消费端写 request.json 与旧路径一致），**退役的是"DSH 直接写 request.json"的旧投递路径**，Hermes 审核侧不变

## 4. 遗留 TODO（非本轮）

- reset_agent.py read_request 补 id/reason 校验（026 非阻塞，另行提审）
- approve 快道 E2E 真实裁决验证（部署后观察一轮审批）
- reset tier E2E（不实际重启的 dry 验证或低风险时机）
