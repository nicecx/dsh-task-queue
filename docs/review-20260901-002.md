# 20260901-002 修订重提：开发前复用检索规范 v2（响应 036 五意见）

- requestId: 20260901-002（修订 20260901-001 changes-requested 后重提）
- 类型: design（复用评估强制关卡 v2）

## 036 五意见逐条落实

### 1. [changeFiles/文档不一致 + "可选 P1"矛盾] → 统一：强制校验在 core.js 纯函数
- **实现落点修正**：`src/core.js` 新增纯函数（可单测），`src/index.js` 的 submit/tool execute 调用：
  ```js
  // core.js
  export function checkReuseSection(content) {
    // 返回 { ok, reason }：设计文档必须含复用评估节 + 至少一条引用；"无复用"声明为合法逃逸
  }
  ```
- 移除全部"可选 P1"表述（强制关卡，无可选）
- changeFiles 统一：src/core.js + src/index.js + test/index.test.js + CAPABILITY-INDEX.md + DSH-HERMES-CONTRACT.md

### 2. [缺"无复用"合法逃逸口] → 加逃逸声明
- `checkReuseSection` 判定规则（大小写不敏感）：
  - 通过条件（任一）：
    a. 含 `## 复用评估` 节 且 节内含至少一条：`CAPABILITY-INDEX` / `awesome` / `github.com` 引用
    b. **合法逃逸**：节内含 `无复用` 或 `无可复用` 声明（全新功能，新造理由充分）→ 视为通过（audit 记 `reuse-escape`）
  - 不通过：缺节 或 节内无引用且无逃逸声明 → 拒绝入队（错误提示列出三查提示 + 逃逸声明写法）

### 3. [缺自动化测试] → 单测纳入 changeFiles
- `test/index.test.js` 新增用例（core.js 纯函数）：
  - 含复用评估节 + CAPABILITY-INDEX 引用 → ok
  - 含 github.com 链接 → ok
  - 缺节 → not ok（含提示）
  - 节名大小写不敏感（`## 复用评估` / `## 复用评估` 变体）
  - 逃逸口：`无复用` 声明 → ok
  - 节空且无逃逸 → not ok

### 4. [节名不统一] → 统一为"复用评估"
- 契约条款同步：三查结果写进设计文档 **`## 复用评估`** 节（删除"既有资产与借鉴"表述）
- 校验/模板/文档全文统一用"复用评估"

### 5. [reuseCheck 落点] → core.js buildRequest 新增字段
- `core.js` 的 buildRequest 新增 `reuseCheck` 字段（`{index: [], awesome: [], github: []}`），由 submit 调用时传入
- Hermes review-inbox 评审清单加"核对 reuseCheck 与复用评估节一致"一项（Hermes 侧备注，changeFiles 范围外）

## 实施清单（approved 后）

1. core.js：`checkReuseSection(content)` 纯函数 + `buildRequest` 加 reuseCheck 字段
2. index.js：submit() 入队前调用 checkReuseSection（文档内容读 docPath 或快照）→ 失败拒绝入队；入队 payload 带 reuseCheck
3. test/index.test.js：上述 6 用例
4. CAPABILITY-INDEX.md 落盘 ~/.dsh/（v0.1）
5. 契约"开发前复用检索"条款（节名统一"复用评估"）
6. 入队关卡生效后验证：新提审缺节被拒（含错误提示）、逃逸口可用

## 变更文件

- dsh-design-review/src/core.js（checkReuseSection + buildRequest.reuseCheck）
- dsh-design-review/src/index.js（submit 关卡调用）
- dsh-design-review/test/index.test.js（6 用例）
- ~/.dsh/CAPABILITY-INDEX.md（新建）
- ~/.dsh/DSH-HERMES-CONTRACT.md（条款）

## 测试

- node --test test/index.test.js（10 旧 + 6 新）
